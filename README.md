# Apple_leaf_disease_project
A convolutional neural network (CNN) model for detecting apple leaf diseases from images. The project includes model training, saved weights, and a GUI for easy prediction.
